# Codex note — кабина Павла, 2026-08-29 (Cursor)

Рабочая папка: `/Users/nateglukhov/analog-horror-site`.
Живой статус: `docs/AGENT_STATUS.md`. Grok Build не вызывать. Commit/push/публикация
только если пользователь попросит в **этом** чате Codex.

Не опираться на память старых чатов. Это снимок того, что Cursor уже сделал
в локальном дереве (часто **не** в HEAD `14bba88`).

---

## Сейчас

Этап: **TOUR LAYER APPROVED / NOT IN GRAPH**.
Замок на момент заметки: смотри `docs/AGENT_STATUS.md` (должен быть `FREE`).
Ключ сейва **не менять:** `tyndex_pavel_observation_booth_v1`.

Production copy + UI-ритм: `docs/PAVEL_OBSERVATION_BOOTH_CONTENT.md`
(раздел **«Тур»** утверждён 2026-08-29).
Канон Павла вне репо: `~/md_lore/pavel.md` — не читать вслух в игре.

---

## Что уже в дереве (не переписывать с нуля)

### Шелл как у Красной комнаты

- `locations/pavel-observation-booth.html` — fullscreen-слой, не страница с нав/футером в игровом кадре.
- `css/pavel-observation-booth.css` — `100dvh`, квадратный stage `cover` по центру, панель реплики снизу.
- HUD: два квадрата **44×44** на кадре (динамик + красный **×**). Жирных «ЗВУК: ВЫКЛ» / «ПОКИНУТЬ КАБИНКУ» больше нет.
- `js/pavel-observation-booth.js` — `body.pavel-booth-open`; мысль при `speaker: "Я"` (пузырь, без таблички); `СИСТЕМА` / `ЗАПИСКА` — system.
- `js/app.js` — снимает `pavel-booth-open`, если корня кабинки нет.

Квадратный кроп 16:9 **намеренный**: композиция центральная. Не «чинить» обратно в stamp-16:9 внутри колонки сайта.

### Текстовый ритм как у Ирины / Лоры

- `content/pavel/observation-booth-content.js` — **51** узел, все reachable.
- На экране **одна** `text` + клик. Поля `action` / `transcript` / оверлей `КАДР:` сняты.
- Старые `nodeId` сохранены как первые биты цепочек (resume не ломается).
- Validator: `node scripts/validate-pavel-observation-booth.js` (лимит длины `text` 180).
- Copy Desk: `scripts/smoke-copydesk.js` ждёт мысли и `ЗАПИСКА`; `lockedSpeakers` включает `ЗАПИСКА`.

Текущий **сюжет сигналов** (смех → слив → склад → люк → кассета → слепой канал → горка → Проводница) уже нарезан. Не склеивать обратно в простыни.

### Медиа линии сигналов

Волны 1–2 mp4 уже в `assets/guest/locations/pavel/` (`loop=false`, `ended` → hold).
`CONTROL_BASE` на intro **не** играет `control-empty` (пустой пост — после ухода Павла).
Клип не рестартить при соседних узлах с тем же visual (mediaKey без `nodeId`).

Пилот PixVerse M01/M03/M09 = REJECT. Не копировать из `projects/pavel-observation-booth/assets/videos/`.

---

## Тур (утверждён, в графе нет)

Пользователь подтвердил: тур — слой **до** `control-laugh`. Живая картинка (~10 с, lock-off: ходит, трогает стену, двигает банки) + короткие диалоги. Лор из `pavel.md` **размазать**, не вывалить.

Хребет: пост → спальня (ящик закрыт) → санузел («трубы») → склад (банки, **горки нет**) → люк (записку не читать) → пост → нынешний смех.

Редкий узел: **банки на складе** → вспышка про дом/родителей/еду-как-заботу. Не факт Агнца, не судьба родителей.

Слив **после** тура обязан противоречить Павлу. Игрок не выбирает канон.

IMG12 (boards, не runtime):  
`/Users/nateglukhov/.codex/generated_images/01a04e61-b1f3-77f3-b42e-5882ebc41a66`  
12 PNG ~1672×941. Павел в кадре: пост `c438cae0`, слив `cc676738`, склад `a8284281`, спальня сидя `53f4588e`. Люка с Павлом нет. `ca7df23f` = REJECT (чужая спальня).  
Видео с `c438cae0` лицом/наездом **не снимать** (риск M01): только wide, мелкая возня в том же кадре.

---

## Не делать без новой фразы пользователя

- commit, push, deploy, публикация;
- PixVerse / shot-лист 10 с / quote;
- вшивать узлы тура в `observation-booth-content.js`;
- менять `tyndex_pavel_observation_booth_v1` и прочие ключи из статуса;
- откатывать чужой грязный diff (`js/app.js`, Солнышко, досье, Copy Desk);
- открывать горку на туре; читать `pavel.md` монологом;
- чистить весь `localStorage`.

Следующий gate (одно из двух, не оба сразу, пока не скажут):

1. узлы тура в content (ритм коротких битов, как сейчас);
2. shot-лист 10 с по first frames IMG12 / matrix.

---

## Проверки, которые уже проходили на шелле

`node --check` JS; validator 51/51; `smoke-copydesk`; `git diff --check`;
browser 1280 и `390×844`: квадрат, HUD 44×44, overflow 0, console 0.
